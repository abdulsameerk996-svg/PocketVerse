{
  "//": "Standalone on purpose, like tools/sprite-sim. The app tsconfig extends expo/tsconfig.base which is incompatible with the classic node resolution this CommonJS build needs. Only pure modules are compiled — the game module graph itself cannot load in Node (react-native is a Metro-only module), so the harness reads the module metadata from the same source files the app boots from.",
  "compilerOptions": {
    "outDir": "build",
    "rootDir": "../../src",
    "module": "commonjs",
    "target": "es2020",
    "moduleResolution": "node",
    "strict": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "baseUrl": "../..",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "files": [
    "../../src/core/types.ts",
    "../../src/core/registry/types.ts",
    "../../src/ui/theme/tokens.ts",
    "../../src/ui/assets/sprites.ts"
  ]
}
